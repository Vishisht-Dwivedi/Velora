# Velora
High-performance event-driven messaging engine built in C with concurrency, synchronization, and realtime distributed communication. It uses nonblocking TCP sockets, epoll-based event loops, custom binary protocols, and multithreaded worker systems to power scalable pub-sub style communication between services and clients.
